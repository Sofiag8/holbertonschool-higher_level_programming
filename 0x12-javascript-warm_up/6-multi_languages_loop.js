#!/usr/bin/node
/* script that prints three lines with specific text,
using just one consele.log and a loop */
const phrases = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (const phrase of phrases) {
  console.log(phrase);
}
